export const CSS = `
:root {
    --consonant-merch-card-three-in-one-width: 365px;
}

merch-card[variant="three-in-one"] {
    min-width: var(--consonant-merch-card-three-in-one-width);
    background-color: var(--spectrum-gray-50);
    border-radius: 8px;
}

`;