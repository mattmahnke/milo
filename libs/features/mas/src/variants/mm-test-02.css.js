export const CSS = `
:root {
    --consonant-merch-card-mm-test-02-width: 365px;
}

merch-card[variant="mm-test-02"] {
    min-width: var(--consonant-merch-card-mm-test-02-width);
    background-color: var(--spectrum-gray-50);
    border-radius: 8px;
}

`;